#ifndef READ_STRING
#define READ_STRING

#include <string>
// Get number from string input 
// Using istringstream
// Throw error if does not contain number
double getDouble(std::string str);

#endif // !READ_STRING
