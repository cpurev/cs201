#include "boolDiv.hpp"

//See bool.Div.hpp
bool isDiv3(const int& n) {
	if (n % 3 == 0)
		return true;
	return false;
}
bool isAdivB(const int& a, const int& b) {
	if (a % b == 0)
		return true;
	return false;
}